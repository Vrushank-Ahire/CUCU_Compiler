int counter = 0;
int incrementCounter();
int main() {
    int num = 10;
    int total = 0;

    char * hello;

    while (i==2) {
        total = i / 2;
    }

    printf("Total: ", total);

    return 0;
}

int incrementCounter() {
    counter = counter+1;
    printf("Total: ", total);
}